import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E71D36',
    },
    secondary: {
      main: '#2EC4B6',
    },
    error: {
      main: '#FF0000',
    },
  },
});

export default theme;
