import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E71D36',
    },
    secondary: {
      main: '#19857b',
    },
    error: {
      main: '#FF0000',
    },
  },
});

export default theme;
